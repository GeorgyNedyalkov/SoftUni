using System;

class SquareRoot
{
    static void Main()
    {
        // Declare new int variable and give it a value
        int number = 12345;

        // Use Math.Sqrt method to calculate the Square Root of given int value
        Console.WriteLine(Math.Sqrt(number));
    }
}