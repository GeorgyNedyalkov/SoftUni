using System;

class VariableInHexadecimalFormat
{
    static void Main()
    {
        // Declare an integer variable with the value 254 in hexadecimal format
        int number = 0xFE;

        // Print it on the console
        Console.WriteLine(number);
    }
}